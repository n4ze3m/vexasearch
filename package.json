{
  "name": "vexasearch",
  "version": "0.0.1",
  "description": "Search engine using image",
  "scripts": {
    "ui:dev": "pnpm run --filter ui dev",
    "api:dev": "pnpm run --filter api dev",
    "dev": "pnpm --stream -r run dev",
    "build": "pnpm --stream -r run build"
  },
  "keywords": [],
  "author": "n4ze3m",
  "license": "MIT",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}